const CachedAttribute = require('./src/traits/CachedAttribute');
const NoTimestamp = require('./src/traits/NoTimestamp');
const Singleton = require('./src/traits/Singleton');

module.exports = {
  CachedAttribute,
  NoTimestamp,
  Singleton,
};
